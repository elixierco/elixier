apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "schemaregistry.fullname" . }}-test-connection"
  labels:
    {{- include "schemaregistry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "schemaregistry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
