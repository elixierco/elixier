{{ if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    {{- include "minio.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  name: {{ include "minio.fullname" . }}
spec:
  rules:
  - host: minio.{{ .Values.global.ingressDomain }}
    http:
      paths:
      - backend:
          service:
            name: {{ include "minio.fullname" . }}
            port:
              number: 9000
        path: /
        pathType: Prefix
  - host: minio-console.{{ .Values.global.ingressDomain }}
    http:
      paths:
      - backend:
          service:
            name: {{ include "minio.fullname" . }}
            port:
              number: 9001
        path: /
        pathType: Prefix
  tls:
    - secretName: {{ include "minio.fullname" . }}
      hosts:
      - minio.{{ .Values.ingress.domain }}
    - secretName: {{ include "minio.fullname" . }}-console
      hosts:
      - minio-console.{{ .Values.global.ingressDomain }}
{{- end }}

