{{ if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    {{- include "ranger.labels" . | nindent 4 }}
    component: ranger
    subcomponent: admin
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  name: {{ include "ranger.fullname" . }}-web
spec:
  rules:
  - host: ranger.{{ .Values.global.ingressDomain }}
    http:
      paths:
      - backend:
          service:
            name: {{ include "ranger.fullname" . }}
            port:
              number: 6080
        path: /
        pathType: Prefix
  tls:
    - secretName: {{ include "ranger.fullname" . }}
      hosts:
      - ranger.{{ .Values.global.ingressDomain }}
{{- end }}
